import * as S from "./ContainerPokemon.style";

const ContainerPokemon = ({ children }) => {
  return <S.ContainerPokemonContainer>{children}</S.ContainerPokemonContainer>;
};

export default ContainerPokemon;
